#include <stdio.h>
int main()
{
    int n,sum,i,a,b,c,d,e;
    scanf("%d",&n);
    sum = 0;
    for (i = n; i <= n; i++)
    {
        if (n%10)
        {
            printf("%d",i);
        }
        
    }
    
}