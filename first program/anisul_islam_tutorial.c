#include <stdio.h>
int main()
{
    // lower to upper
    /*int lower;
    scanf("%c",&lower);
    printf("%c",lower - 32);
    */

    // upper to lower
    /*int upper;
    scanf("%c",&upper);
    printf("%c",upper+32);

   //using lib function:
   int upper, lower;
   scanf("%c",&upper);
   //upper = toupper(lower);
   lower = tolower(upper);
   printf("%c",lower);*/

    // Decimal to Octal && Hexadecimal: %o && %x

    //   int number;
    //   scanf("%d",&number); //input nicchi decimal
    // printf("%o",number); //output octal
    // printf("%x",number); // output hexadecimal

    //   float a,b,c,s,area;
    //   scanf("%f%f%f",&a,&b,&c);

    //   s = (a+b+c)/2;
    //   area= sqrt(s*(s-a)*(s-b)*(s-c));
    //   printf("%.2f",area);

    // int a,b;
    // scanf("%d%d",&a,&b);
    // a = a-b;
    // b = a+b;
    // a = b-a;
    // printf("%d\n%d",a,b);
}